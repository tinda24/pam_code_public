_target_: modules.memory_router.his_perceiver.HisPerceiver
hidden_dim: ${agent.hidden_dim}
q_type: ${agent.q_type}
global_token_length: 3
num_heads: 8
layers: 4
dropout: 0.1

use_avg_patch_token: False
